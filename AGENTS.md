# Agent Instructions

- Keep deterministic behavior for mock upstream logic.
- Ensure `python -m scripts.run_all` remains the canonical entrypoint.
- Commit generated `results/report.csv`, `results/report.md`, and `results/plots/*.png`.
